[project]
name = "pcbpartparse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "polars>=1.29.0",
    "pydantic>=2.11.4",
    "pydantic-settings>=2.9.1",
    "python-dotenv>=1.1.0",
    "textual>=3.2.0",
]

[project.scripts]
pcbpartparse = "pcbpartparse:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
