# pcbpartparse

## Example python lib for parsing eagle pcb csv data exports

### Usage

Install and run via 'pcbpartparse' command. Modify the 'settings.json' file to your desired specifications and place it in your working directory.

### Config

Use 'settings.json' to specify input/output directories and dictionary for part grouping.
 